{
    "private": true,
    "type": "module",
    "scripts": {
        "serve": "php artisan serve",
        "build": "cp .env.example .env && npm install && composer install && composer dump-autoload && php artisan key:generate && php artisan db:create && php artisan migrate && php artisan db:add-admin-user && php artisan db:seed-default && php artisan serve"
    },
    "devDependencies": {
        "autoprefixer": "^10.4.20",
        "axios": "^1.7.4",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^1.0",
        "postcss": "^8.4.47",
        "tailwindcss": "^3.4.13",
        "vite": "^5.0"
    },
    "dependencies": {
        "jquery": "^3.7.1"
    }
}
